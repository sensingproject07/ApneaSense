.venv/
__pycache__/

# Jupyter notebook checkpoints
.ipynb_checkpoints/

# Model weights — too large for git (GitHub 100 MB hard limit, 50 MB warning).
# Production weights live in GitHub Releases (see model cards in artifacts/);
# training-time checkpoints are reproducible from the source notebooks.
*.pth
*.pt

# Synthetic depth cache from inference experiments (thousands of PNGs)
experiments/artifacts/vision-spatial/inference_experiments/synthetic_depth_eval/depth_synth_cache/

# MediaPipe model binary (30 MB — download via synthetic_joints_eval.ipynb)
*.task

# Claude Code local state
.claude/
